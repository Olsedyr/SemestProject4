import React, { useState, useEffect } from 'react';



const prodPdf = () => {

    const savePdf = () => {
        // Implement logic for aborting
    };

    return (
        <div className="controls">
            <button className="button" onClick={savePdf}>save PDF</button>
        </div>
    );
};

export default prodPdf;